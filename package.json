{
  "name": "vibeforce",
  "private": true,
  "version": "0.1.0",
  "description": "An open-source agent harness for Salesforce development",
  "license": "MIT",
  "scripts": {
    "build": "pnpm --filter @vibeforce/core build && pnpm --filter @vibeforce/cli build",
    "dev": "pnpm --filter @vibeforce/core dev & pnpm --filter @vibeforce/cli dev",
    "start": "node apps/cli/dist/index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
